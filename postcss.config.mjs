/**
 * Tailwind CSS v4 (via @tailwindcss/vite) configura da solo i plugin PostCSS
 * necessari — non serve includere qui `tailwindcss` o `autoprefixer`.
 *
 * Questo file resta per eventuali plugin PostCSS aggiuntivi in futuro.
 */
export default {}
